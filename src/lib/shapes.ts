/**
 * The chamfered octagon every round control on the site is cut from — the
 * close button and the listing's forward arrow share one silhouette so the
 * two read as the same family. Drawn in a 60x60 viewBox.
 */
export const OCTAGON_PATH =
  "M60 47.5 L47.5 60 L12.5 60 L0 47.5 L0 12.5 L12.5 0 L47.5 0 L60 12.5 Z";
